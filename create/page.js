$(function() {
  Dropbox.choose({
    success: function(files) {
       
       //data = JSON.stringify(files[0].link);
       //console.log(data)
       //BL.createContent(data);
    }
  })
});
